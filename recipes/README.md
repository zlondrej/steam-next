# Recipes

Each directory contains recipes using the ``steam-next`` package.
See the individual ``README`` for details.

All recipes assume the environment is already set up.
In order not to break any system packages we will use ``virtualenv``.

```bash
virtualenv env
source env/bin/activate
pip install steam-next
```
