name: Run tests

on:
  push:
    branches:
      - master
    paths-ignore:
      - '.github/**'
      - '.gitignore'
      - '.gitattributes'
      - '*.md'
      - '*.rst'
      - 'LICENSE'
      - 'protobuf_list.txt'
      - 'docs/**'
      - 'protobufs/**'
      - 'recipes/**'
  pull_request:
    branches:
      - master
    paths-ignore:
      - '.github/**'
      - '.gitignore'
      - '.gitattributes'
      - '*.md'
      - '*.rst'
      - 'LICENSE'
      - 'protobuf_list.txt'
      - 'docs/**'
      - 'protobufs/**'
      - 'recipes/**'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  POETRY_VERSION: 2.2.1

jobs:
  test:
    strategy:
      fail-fast: false
      matrix:
        os:
          - "ubuntu-latest"
          - "macos-latest"
          - "windows-latest"
        python-version:
          - "3.10"
          - "3.11"
          - "3.12"
          - "3.13"
          - "3.14"
    runs-on: ${{ matrix.os }}
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v6

      - name: Set up python environment
        uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python-version }}

      - name: Display Python version
        run: python -c "import sys; print(sys.version)"

      - name: Install dependencies
        run: |
          pipx install poetry==${{ env.POETRY_VERSION }}
          poetry install --extras client --with dev --no-interaction --no-ansi

      - name: Run Tests
        run: poetry run pytest -p no:cov